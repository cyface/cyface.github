{
  "name": "cyface.github",
  "version": "1.0.0",
  "description": "Cyface Design Website Publisher",
  "author": "Tim L. White",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "push-dir --dir=dist --branch=gh-pages --remote=pages --cleanup --verbose --local-branch-name=deploy"
  },
  "dependencies": {
    "cross-env": "^5.2.0",
    "npm-cli": "^0.1.0",
    "nuxt": "^2.4.0",
    "push-dir": "^0.4.1",
    "vue": "^2.5.22",
    "vuetify": "^1.3.14",
    "vuetify-loader": "^1.0.8"
  },
  "devDependencies": {
    "nodemon": "^1.18.9",
    "stylus": "^0.54.5",
    "stylus-loader": "^3.0.2"
  }
}
